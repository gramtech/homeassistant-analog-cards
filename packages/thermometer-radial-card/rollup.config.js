import { makeCardConfig } from '../../tools/rollup-card.mjs';

export default makeCardConfig({
  input: 'src/analog-thermometer-radial-card.ts',
  outputFile: 'analog-thermometer-radial-card.js',
});
